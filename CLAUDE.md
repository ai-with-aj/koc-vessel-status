# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**vessel-status** — a new project initialized on 2026-05-22. No source files exist yet; update this file as the codebase grows.

## Getting Started

Once code is added, document the build/run/test commands here, for example:

```
# Install dependencies
npm install          # or: pip install -r requirements.txt / cargo build / etc.

# Run the project
npm start

# Run tests
npm test

# Lint
npm run lint
```

Replace the placeholders above with the actual commands once the tech stack is decided.

## Architecture

Document the high-level architecture here as it takes shape:
- Entry point(s)
- Major modules / services
- Data flow between components
- External dependencies / APIs
